# Core data
pandas>=2.2
pyarrow>=17.0
numpy>=2.0

# Mining
mlxtend>=0.23        # Apriori, FP-Growth
prefixspan>=0.5.2    # pure-Python fallback for PrefixSpan; SPMF Java is primary

# Modeling / eval
scikit-learn>=1.5
scipy>=1.13

# Fetch / IO
kaggle>=1.6          # Azure PdM dataset

# Dev / notebooks
jupyter>=1.0
matplotlib>=3.9
seaborn>=0.13
pytest>=8.0
tqdm>=4.66
tabulate>=0.9         # pd.DataFrame.to_markdown()
